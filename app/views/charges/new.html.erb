<h1>Thank you for buying food from @RubiServes, <%= current_user.name %></h1>
<h4>Here on you will be directed to the third-party payment gateway called Stripe</h4>
<h5>We hope you enjoy our food!!</h5>
<%= form_tag charges_path do %>
    <article>
      

      <label class="amount">
        <span>Amount: Rs.<%= current_ordeer.subtotal %></span>
      </label>
    </article>

    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
            <%= hidden_field_tag 'id',@Ordeer.id %>
            data-key="<%%= Rails.configuration.stripe[:publishable_key] %>"
            data-description="Payment for the Food Item"
            data-amount=<%= current_ordeer.subtotal %>
            data-locale="auto">
    </script>
<% end %>